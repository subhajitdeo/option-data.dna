name: Save Trades to GitHub

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  save-trades:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Create data directory
        run: mkdir -p data
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install requests
        run: pip install requests
      
      - name: Run trade saver
        run: python scripts/save_trades.py
      
      - name: Commit and push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/trades_history.json
          git diff --staged --quiet || git commit -m "Update trades - $(date -u '+%Y-%m-%d %H:%M:%S')"
          git pull origin main --no-edit --allow-unrelated-histories || true
          git push origin main || echo "Push skipped - conflict will resolve next run"
